import { Stack, Typography } from "@repo/ui";

export function OurWhyStory() {
  return (
    <Stack spacing="4">
      <Typography variant="body-lg">
        Commerce has changed.
      </Typography>

      <Typography variant="body-lg">
        Communities have changed.
      </Typography>

      <Typography variant="body-lg">
        Technology has changed.
      </Typography>

      <Typography variant="body-lg">
        Yet millions of African businesses are still
        forced to choose between visibility,
        trust and ownership.
      </Typography>

      <Typography variant="body-lg">
        We believe they deserve all three.
      </Typography>

      <Typography variant="body-lg">
        DUWEM is our attempt to build a different
        kind of commerce platform.
      </Typography>

      <Typography variant="body-lg">
        One where businesses don't just sell.
      </Typography>

      <Typography variant="body-lg">
        They build communities.
      </Typography>

      <Typography variant="body-lg">
        Communities build trust.
      </Typography>

      <Typography variant="body-lg">
        And trust creates lasting commerce.
      </Typography>
    </Stack>
  );
}