import { Typography } from "@repo/ui";

export function HeroDescription() {
  return (
    <>
      <Typography variant="body-lg">
        We're building Africa's next generation social
        commerce platform alongside merchants,
        creators, developers and everyday shoppers.
      </Typography>

      <Typography variant="body-lg">
        Because the best platforms aren't built for
        communities.
      </Typography>

      <Typography variant="body-lg">
        They're built with them.
      </Typography>
    </>
  );
}