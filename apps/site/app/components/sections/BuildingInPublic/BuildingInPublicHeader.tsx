import { Stack, Typography } from "@repo/ui";

export function BuildingInPublicHeader() {
  return (
      <Stack spacing="4">
      <Typography variant="display-md">
        Building in Public
      </Typography>

      <Typography variant="body-lg">
        Every conversation, every survey response,
        every prototype and every milestone helps
        shape DUWEM.
      </Typography>
    </Stack>
  );
}