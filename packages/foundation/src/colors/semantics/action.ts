import { purple } from "../primitives/purple";
import { teal } from "../primitives/teal";

export const action = {
  primary: purple[600],
  primaryHover: purple[700],
  primaryActive: purple[800],

  secondary: teal[600],
  secondaryHover: teal[700],
  secondaryActive: teal[800],
} as const;